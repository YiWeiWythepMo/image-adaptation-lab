{
  "version": "0.1",
  "locked_object_types": [
    "product",
    "fifa_world_cup_trophy"
  ],
  "locked_object_rules": {
    "allowed_actions": ["move", "scale_uniform"],
    "forbidden_actions": ["redraw", "regenerate", "reshape", "recolor", "non_uniform_scale"]
  },
  "decoration_rules": {
    "logo": "preserve_if_possible",
    "text": "preserve_if_simple_otherwise_rebuild_outside_pipeline"
  },
  "background_rules": {
    "allow_extension": true,
    "extension_scope": "newly_added_regions_only"
  },
  "strategy_preference": [
    "scale_only",
    "scale_plus_padding",
    "scale_plus_padding_plus_background_extension"
  ],
  "output_requirements": [
    "image_analysis",
    "recommended_strategy",
    "reason",
    "risk_level"
  ]
}